info:
  name: Search Manga
  type: http
  seq: 3

http:
  method: get
  url: "{{PROD}}/api/search?q=chainsaw-man"
  params:
    - name: q
      value: chainsaw-man
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
